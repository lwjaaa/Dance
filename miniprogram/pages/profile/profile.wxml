<view class="profile">
  <view class="bg"></view>
  <user-infor userInfor="{{userInfor}}" />
  <user-videos 
  videos="{{userVideos}}"
  tabIndex="{{tabIndex}}" bind:switch-tab="changeVideos" />

  <!-- <tabbar tab="{{'/pages/profile/profile'}}" /> -->
</view>